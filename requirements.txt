cryptography>=48.0.0
certifi>=2025.11.12
pygame-ce>=2.5.7
pygame_gui>=0.6.14
python-i18n>=0.3.9
pyperclip>=1.11.0
typing-extensions>=4.15.0

# Build tools - installed separately by each build script (not via pip install -r)
# ordered-set and zstandard are pulled in automatically as Nuitka dependencies.
